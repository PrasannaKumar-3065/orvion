# ============================================================
#  Orvion — Requirements (API-inference build)
#
#  This project runs in API mode only — all AI inference is
#  handled remotely via gradio_client → HuggingFace Space.
#  PyTorch / transformers / unsloth are NOT required.
#
#  Install:
#    pip install -r requirements.txt
# ============================================================


# ── UI Framework ─────────────────────────────────────────────
PyQt5>=5.15.10
PyQtWebEngine>=5.15.7


# ── Image Processing ─────────────────────────────────────────
#  PIL.Image used in agent_worker.py (screenshots) and
#  first_run_bootstrap.py (icon generation)
Pillow>=10.0.0


# ── System Monitoring ────────────────────────────────────────
psutil>=5.9.0


# ── HTTP / Networking ────────────────────────────────────────
requests>=2.31.0


# ── HuggingFace Space Client (API inference) ─────────────────
#  gradio_client is the ONLY HuggingFace dep needed.
#  It calls the remote Space over HTTP — no local model needed.
gradio_client>=0.7.0


# ── .env loading ─────────────────────────────────────────────
#  agent_worker.py calls load_dotenv() to read HF_TOKEN
python-dotenv>=1.0.0


# ── General Utilities ────────────────────────────────────────
packaging>=23.0


# ── Build Tool (not needed to run from source) ───────────────
# pyinstaller>=6.0.0